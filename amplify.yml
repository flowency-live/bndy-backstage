version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  # Cache temporarily disabled to force fresh npm install
  # cache:
  #   paths:
  #     - node_modules/**/*